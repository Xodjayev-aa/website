fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
groq>=0.31,<1
pydantic>=2.11,<3

# Auth: Google OAuth + JWT session cookie
authlib>=1.3,<2
itsdangerous>=2.1,<3
python-jose[cryptography]>=3.3,<4
httpx>=0.27,<1

# Database: per-user tier + daily usage quotas
sqlalchemy>=2.0,<3
aiosqlite>=0.20,<1

# Billing
stripe>=10.0,<12

# Async helper used to run the sync Stripe SDK off the event loop
anyio>=4.0,<5
